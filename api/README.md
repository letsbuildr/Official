## Setup

1. Copy the example environment file and create a new config.env file locally in /api
   cp api/.env.example api/config.env
2. Fill in your environment variables
   your environment variables include your email service, your email password, your google client id, and your database url
3. Run the server using npm run start. Server will run on port 5000
